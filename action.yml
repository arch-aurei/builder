name: "archlinux package builder"
description: "I build packages"
inputs:
  action:
    description: 'build or package'
    required: true
runs:
  using: 'docker'
  image: 'docker://docker.pkg.github.com/arch-aurei/builder/aurei-builder:latest'
  args:
    - "--${{ inputs.action }}"
